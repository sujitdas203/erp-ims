@model IMS.Models.ViewModels.AdmissionApplicationFormViewModel

@if (Model.AA_Id.HasValue) { @Html.HiddenFor(m => m.AA_Id) }

<div class="row g-3">
    <div class="col-md-6">
        <label class="form-label">Branch <span class="text-danger">*</span></label>
        <select id="AA_BranchId" name="AA_BranchId" class="form-select" data-selected-value="@(Model.AA_BranchId.ToString())"></select>
    </div>
    <div class="col-md-6">
        <label class="form-label">Application Number</label>
        @Html.TextBoxFor(m => m.AA_ApplicationNumber, new { @class = "form-control", placeholder = "Auto-generated if blank" })
    </div>
    <div class="col-md-4">
        <label class="form-label">First Name <span class="text-danger">*</span></label>
        @Html.TextBoxFor(m => m.AA_FirstName, new { @class = "form-control" })
    </div>
    <div class="col-md-4">
        <label class="form-label">Last Name <span class="text-danger">*</span></label>
        @Html.TextBoxFor(m => m.AA_LastName, new { @class = "form-control" })
    </div>
    <div class="col-md-4">
        <label class="form-label">Phone <span class="text-danger">*</span></label>
        @Html.TextBoxFor(m => m.AA_Phone, new { @class = "form-control" })
    </div>
    <div class="col-md-4">
        <label class="form-label">Email</label>
        @Html.TextBoxFor(m => m.AA_Email, new { @class = "form-control", type = "email" })
    </div>
    <div class="col-md-4">
        <label class="form-label">Date of Birth</label>
        @Html.TextBoxFor(m => m.AA_DateOfBirth, "{0:yyyy-MM-dd}", new { @class = "form-control ims-datepicker-input", placeholder = "DD/MM/YYYY", @id = "AA_DateOfBirth" })
    </div>
    <div class="col-md-4">
        <label class="form-label">Gender</label>
        <select id="AA_Gender" name="AA_Gender" class="form-select" data-selected-value="@(Model.AA_Gender)"></select>
    </div>
    <div class="col-md-4">
        <label class="form-label">Course</label>
        <select id="AA_CourseId" name="AA_CourseId" class="form-select" data-selected-value="@(Model.AA_CourseId?.ToString())"></select>
    </div>
    <div class="col-md-4">
        <label class="form-label">Academic Year <span class="text-danger">*</span></label>
        <select id="AA_AcademicYearId" name="AA_AcademicYearId" class="form-select" data-selected-value="@(Model.AA_AcademicYearId.ToString())"></select>
    </div>
    <div class="col-md-4">
        <label class="form-label">Status</label>
        <select id="AA_Status" name="AA_Status" class="form-select" data-selected-value="@(Model.AA_Status)"></select>
    </div>
    <div class="col-md-12">
        <label class="form-label">Notes</label>
        @Html.TextAreaFor(m => m.AA_Notes, new { @class = "form-control", rows = "3" })
    </div>
</div>

<div class="d-flex justify-content-end gap-2 mt-4">
    <a asp-controller="AdmissionApplication" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
    <button type="submit" class="btn btn-primary"><i class="fa-solid fa-check me-1"></i> Save Application</button>
</div>
